<UserControl x:Class="StoreApp.Core.Views.Views.BookView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dropDownButton="clr-namespace:DropDownButton"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
    <UserControl.Resources>
        <DataTemplate x:Key="frontTemplate">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Name}"></TextBlock>
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="backTemplate">
            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding Name}"></TextBox>
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>
    <Grid Background="Pink">
        <ListBox>
            <ListBox.ItemTemplate>
                 <DataTemplate>
                    <dropDownButton:FlipControl FrontDataTemplate="{StaticResource frontTemplate}" BackDataTemplate="{StaticResource backTemplate}"></dropDownButton:FlipControl>
                 </DataTemplate>
             </ListBox.ItemTemplate>
         </ListBox>   
    </Grid>
</UserControl>
